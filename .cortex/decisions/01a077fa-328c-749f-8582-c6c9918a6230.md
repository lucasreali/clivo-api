---
title: "No reporting module: BillingReport, BillingTotals and ReportPeriod stay inside core/billing"
keywords: ["reporting", "relatorio", "relatório", "BillingReport", "BillingTotals", "ReportPeriod", "billing", "faturamento", "modulo", "module", "arquitetura", "architecture", "README"]
module: "core"
anchors:
  - "src/main/java/com/example/clivoapi/core/billing/BillingReport.java"
  - "src/main/java/com/example/clivoapi/core/billing/BillingTotals.java"
  - "src/main/java/com/example/clivoapi/core/billing/ReportPeriod.java"
  - "README.md"
commit: "72b4b527b2bd913411bdb58236803af76877e84f"
dirty: true
provenance: "agent"
created_at: "2026-09-06T18:28:00.780Z"
---

The product has exactly one report, the financial one, and it reads nothing but invoices. `BillingReport`, `BillingTotals` and `ReportPeriod` therefore live in `core/billing`, beside the data they summarize, and are served by `GET /api/invoices/report`.

Rejected: a `core/reporting` module. It would own no table of its own, would have to depend on billing to say anything at all, and would pull the `RoleAccess.requireFinancialReport` check — which restricts the figures to management — away from the module that enforces it. The extra boundary buys nothing today.

The condition for revisiting is explicit: when a report needs data from more than billing, it earns its own module.

This decision is now stated in `README.md` under "Project layout", so a reader who notices the missing module finds the reason without digging.
